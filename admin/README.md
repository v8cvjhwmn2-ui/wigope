# Wigope Pay — Admin

Next.js 14 (App Router) + Tailwind themed to Wigope tokens.

```bash
cp .env.example .env.local
npm install
npm run dev
```

Theme tokens live in `tailwind.config.ts` and mirror `docs/BRAND.md`. Keep them in sync with `mobile/lib/app/theme/colors.dart`.
